[phases.setup]
nixPkgs = ["python312", "file"]

[phases.install]
cmds = ["chmod +x prepare-env.sh && ./prepare-env.sh"]

[start]
cmd = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
