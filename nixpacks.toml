[phases.setup]
nixPkgs = ["python312", "file"]

[phases.install]
cmds = ["chmod +x prepare-env.sh && ./prepare-env.sh"]

[start]
cmd = "bash -c 'cd /app && python -c \"from alembic.config import main; main()\" upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
